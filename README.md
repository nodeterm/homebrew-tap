# nodeterm Homebrew tap

```bash
brew tap nodeterm/tap
brew trust nodeterm/tap   # Homebrew ≥6 asks once for third-party taps
brew install nodeterm-pair
```

| Formula | What it is |
|---|---|
| `nodeterm-pair` | Host-side QR pairing helper for [nodeterm mobile](https://nodeterm.dev) |
